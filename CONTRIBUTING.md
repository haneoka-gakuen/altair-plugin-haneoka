# Contributing

Open an issue before a large protocol change. Keep source-specific behavior in
this plugin, keep host I/O behind the exported adapter, and include tests for
new resource kinds and cancellation paths.

Run `pnpm check` before submitting a change.
